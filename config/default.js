const config = {
    // 启动端口
    port: 5000,
  
    // 数据库配置
    database: {
      DATABASE: 'demo',
      USERNAME: 'root',
      PASSWORD: '123456',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
  
  module.exports = config